LUPAPISTE.PreambleSummaryModel = function( params ) {
  "use strict";
  var self = this;

  ko.utils.extend( self, new LUPAPISTE.ComponentBaseModel() );

  self.rootApplication = params.rootApplication;
};
